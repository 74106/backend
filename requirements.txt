# Consolidated requirements for this repo (runtime + dev)
# Install with: pip install -r requirements.txt

# Runtime / backend
Flask==3.1.2
Flask-CORS==6.0.1
python-dotenv==1.1.1
requests==2.32.5
google-genai
PyJWT==2.10.1
passlib==1.7.4
reportlab==4.4.4
deep-translator==1.11.4
Werkzeug==3.1.3
googletrans==4.0.0rc1
# Optional / DB drivers
# sqlite3 is in the Python stdlib (no install required)

# Dev / testing / lint
pytest==7.4.0
pytest-cov==4.1.0
mypy==1.8.0
flake8==6.0.0

# Notes:
# - If you deploy to production, prefer installing only the runtime set (remove dev entries) or
#   use a virtualenv per service (backend, backend-2) and install their specific requirement files.
# - Gemini API uses REST via requests; set GEMINI_API_KEY environment variable

